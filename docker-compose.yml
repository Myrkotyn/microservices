version: '3'
services:
  gateway:
    build: ./gateway
    ports:
      - 3001:3001
    networks:
      - backend
  calculator:
    build: ./calc
    networks:
      - backend
  cache:
    build: ./cache
    ports:
      - 3010:3010
    networks:
      - backend

networks:
  backend:
    name: backend
